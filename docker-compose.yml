services:
  backend:
    build: ./backend
    container_name: lastchapter-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./data/last_chapter.db
      - UPLOADS_DIR=/app/uploads
      - SECRET_KEY=${SECRET_KEY:-dev-secret-change-me}
      - AUTH0_DOMAIN=dev-oqsb7nkisppimu66.us.auth0.com
      - AUTH0_CLIENT_ID=osIQVsnIbjmaYqsoUlBUzIr3VMwCg6jT
    volumes:
      - db-data:/app/data
      - uploads-data:/app/uploads
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 10s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./frontend
      args:
        - VITE_API_BASE_URL=/api
        - VITE_AUTH0_DOMAIN=dev-oqsb7nkisppimu66.us.auth0.com
        - VITE_AUTH0_CLIENT_ID=osIQVsnIbjmaYqsoUlBUzIr3VMwCg6jT
    container_name: lastchapter-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  db-data:
  uploads-data:
